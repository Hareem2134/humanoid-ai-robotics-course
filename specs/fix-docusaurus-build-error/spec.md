# Feature: Fix Docusaurus Build Error

## User Stories

### US1: Fix `localStorage` is not defined error

As a developer, I want to fix the Docusaurus build error so that the website can be deployed on Vercel.

**Acceptance Criteria:**
- The `yarn build` command completes successfully without any `localStorage` related errors.
- The website is deployed and accessible on Vercel.
